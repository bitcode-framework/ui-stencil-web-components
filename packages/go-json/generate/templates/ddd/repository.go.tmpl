package {{lower .Model.Name}}

// Repository defines the persistence interface for {{pascal .Model.Name}}.
type Repository interface {
	FindAll() ([]{{pascal .Model.Name}}, error)
	FindByID(id int) (*{{pascal .Model.Name}}, error)
	Create(entity *{{pascal .Model.Name}}) error
	Update(entity *{{pascal .Model.Name}}) error
	Delete(id int) error
}
